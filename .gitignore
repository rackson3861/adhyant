# Node dependencies
node_modules/

# Local env (secrets) – use .env.example as a template
.env
.env.local
.env.development.local
.env.production.local

# Lock file (NOT recommended usually, but if you want to ignore it)
package-lock.json

dist/
